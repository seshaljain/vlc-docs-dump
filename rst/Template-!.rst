\|<noinclude> Special template used to include the pipe character in
template parameters so it doesn't mess up.

== See also == \* <code>{{tl((}}</code> \* <code>{{tl&#x3D;}}</code>

[[Category:Templates]]</noinclude>
